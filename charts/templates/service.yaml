apiVersion: v1
kind: Service
metadata:
  name: {{ include "goapp.fullname" . }}
  labels:
    app: {{ include "goapp.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "goapp.name" . }}
  ports:
    - port: 80
      
